interface:
  display_name: "Markdown Platform Publisher"
  short_description: "Publish Markdown images via PicList/R2"
  default_prompt: "Use $markdown-platform-publisher to turn this Markdown article into a CSDN-ready post with PicList/R2 HTTPS image links."
policy:
  allow_implicit_invocation: true
